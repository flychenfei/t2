<script id="tmpl-DriveFolder" type="text/html">
 <div class="googleDrive DriveFolder">
	  <div class="dialogBackground">
		</div>
		<div class="dialogContent">
			<div class="dialogHead">
				<span class="dialogTitle">Move to...</span>
				<span class="close dialogBtns dialogCloseBtn">&times;</span>
			</div>
			<div class="dialogBody" data-fileId="{{data.fileId}}" data-parentId="{{data.parentId}}">
			 <div class="itemDiv">
			 	<div class="folderitem">
			 		<span class="foldername">My Drive</span>
			 	</div>
			 </div>
		 	{{#each data.result}}
				<div class="itemDiv">
					<div class="folderitem">
					{{#if hasChild}}
						<span class="driveFolderPlus"><i class="icon-plus"></i></span>
					{{else}}
						<span class="driveFolderMinus"><i class="icon-minus"></i></span>
					{{/if}}
						<span class=""><i class="icon-folder-close"></i></span>
						<span class="foldername" data-selfId="{{selfId}}">{{name}}</span>
					</div>
				</div>
			{{/each}}
		 	<div class="dialogBtns">
				<button class="btn btn-primary move">Move</button>
				<button class="btn btn-primary cancel">Cancel</button>
	 		</div>
		</div>
	</div>
</div>
</script>

<script id="tmpl-DriveSubFolder" type="text/html">
	<div class="subfolder itemDiv">
		{{#each data.result}}
				<div class="itemDiv">
					<div class="folderitem" data-Id="{{selfId}}>
					{{#if hasChild}}
						<span class=""><i class="icon-plus"></i></span>
					{{else}}
						<span class=""><i class="icon-minus"></i></span>
					{{/if}}
						<span><i class="icon-folder-close"></i></span>
						<span class="foldername">{{name}}</span>
					</div>
				</div>
			{{/each}}
	</div>
</script>